box: golang
build:
  steps:
    - arjen/hugo-build@1.8.4:
      version: "0.15"
      config: config.toml
deploy:
  steps:
    - install-packages:
      packages: git ssh-client
    - lukevivier/gh-pages@0.2.1:
      token: $GIT_TOKEN
      domain: www.nmcconnell.com
      basedir: build
      repo: FluxAugur/FluxAugur.github.io
